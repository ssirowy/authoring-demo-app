
import { attributedString } from 'authoring-demo-app/helpers/attributed-string';
import { module, test } from 'qunit';

module('Unit | Helper | attributed string');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = attributedString([42]);
  assert.ok(result);
});

